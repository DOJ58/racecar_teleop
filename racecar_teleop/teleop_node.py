"""ROS node for racecar_teleop — NEXT MILESTONE (not yet implemented).

This is a placeholder so the package entry point resolves.  The real
implementation (rclpy node, hold-to-enable state machine, monotonic-clock
input timeout, fixed-rate zero-command publishing) requires ROS 2 Humble and
will be written after the pure mapping layer (``mapping.py``) is finished and
tested.
"""


def main(args=None):
    raise NotImplementedError(
        "racecar_teleop.teleop_node is not implemented yet; "
        "see mapping.py for the finished pure-mapping layer."
    )
